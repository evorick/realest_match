<div class="container">
  <div class="form-signin">
    <h1 class="form-signin-heading">Tell us about yourself</h1>

    <%= render 'form' %>
    <br>
    <% if user_signed_in? %>
      <%= link_to 'Back', listers_path %>
    <% end %>
    <br>
  </div>
</div>